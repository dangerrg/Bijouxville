<%= form_with(model: jeweler) do |form| %>
  <% if jeweler.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(jeweler.errors.count, "error") %> prohibited this jeweler from being saved:</h2>

      <ul>
        <% jeweler.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %><br />
    <%= form.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= form.label :password %><br />
    <%= form.password_field :password %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
